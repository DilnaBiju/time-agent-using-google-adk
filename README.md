# Time Agent using Google ADK 🕒

A simple AI agent built with Google's Agent Development Kit (ADK) that tells the current time in any city using the Gemini 2.5 Flash model.

## Features
- Uses Gemini model via ADK
- Responds with current city time
- Modular tool-based agent setup

## Run Locally
```bash
adk run my_agent
